# InnoForge v0.5.6 发布前多角色检查

## forge-evolver / 风险收束
- 当前版本仍为 `0.5.6`，属于 0.5.x 修复线，不切换 0.6 版本语义。
- 发布范围集中在搜索去重、AI 输出质量门禁、创意流程恢复、设置与发布验证文档。
- 真实测试报告已入库：`docs/release-e2e-summary-v0.5.6-2026-04-24.md`。

## 战略 / aion-zl
- 本轮优先修复“能否正常使用”和“发布前是否真实测试”的信任问题。
- 国内 CNIPR 不再作为默认主链路，保留兼容入口；当前以本地库 + 国外数据源配置为主。
- 重复公开号问题通过规范化键与结果去重处理，不以事后清洗替代根因修复。

## 闭环演进顾问 / Evolution Advisor
- 已将 v0.5.x 发布事故复盘写入 `docs/release-incident-v0.5x.md`。
- 已新增发布门禁文档和验证脚本，避免再次把“可编译”误判为“可发布”。
- 已新增真实页面输入/按钮/输出测试记录，作为发布判断证据。

## aion-forge / 编排检查
- `cargo fmt --all -- --check`：通过。
- `cargo test --lib`：31 项通过。
- `cargo test --tests`：完整通过。
- `cargo clippy --tests -- -D warnings`：通过。
- `cargo check --release`：通过。
- 打包 exe 冒烟：HTTP 200，首页 29802 字节。

## 郝匠 / 工程质量审视
- 发布包路径：`target/package/innoforge-v0.5.6-windows-x86_64.zip`。
- SHA256：`65D55E205C979B2E9CBFD379F93A0D0F69FEEEAA4F75013F0BE47F357CAA0586`。
- `start.bat` 已修正：包目录直接运行 exe，源码目录构建后运行。
- 发布前未发现 `innoforge` 残留进程或 3000/3921 端口占用。

## 朝堂 / 对照结论
- 反对意见：页面覆盖仍有 4 个动态动作跳过。
- 处理结论：跳过项来自动态控件点击后重排或消失，非服务错误；对应页面控制台错误为 0，核心业务断言已通过。
- 发布结论：可发布当前 0.5.x 修复版本。
